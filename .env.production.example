# Copy to .env.production.local and replace with the real HTTPS API host.
# Placeholder and localhost values intentionally fail production validation.
EXPO_PUBLIC_API_BASE_URL=https://api.example.com
